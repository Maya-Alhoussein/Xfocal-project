enum StatusRequest {
  loading,
  success,
  failure,
  serverFailure,
  offlineFailure,
  none
}

//enum : we use it to define type of states for example : accept , pending , refuse we declare it by enum variable
//StatusRequest : the type of states of the server
