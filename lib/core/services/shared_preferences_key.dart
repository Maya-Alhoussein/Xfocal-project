class SharedPreferencesKeys {

  static String tokenKey = "token" , isLoginKey = "login";
  static const String themeKey = 'isDarkMode';

}

//shared preferences : store data in cache (so if we close the app => we don't loose the data)